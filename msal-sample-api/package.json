{
  "dependencies": {
    "@azure/identity": "^4.4.1",
    "@azure/msal-node": "^2.13.0"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "node dist/msalApi.js",
    "test": "echo \"No tests yet...\""
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
